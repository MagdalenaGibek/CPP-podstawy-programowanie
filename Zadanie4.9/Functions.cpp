#include <iostream>
#include <string>

std::string concatenateStrings(std::string s[], const int len, char c = '\n')
{

    std::string str;

    for (int i = 0; i < len; ++i)
    {
        if (c == ';')
        {
            std::cout << std::endl;
        }
    }
}